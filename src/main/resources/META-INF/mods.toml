modLoader = "javafml" #mandatory
loaderVersion = "${loader_version_range}" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
properties.
default here.
license = "${mod_license}"
[[mods]] #mandatory
modId = "${mod_id}" #mandatory
version = "${mod_version}" #mandatory
displayName = "${mod_name}" #mandatory
https://docs.minecraftforge.net/en/latest/misc/updatechecker/
authors = "${mod_authors}" #optional
behaviour and should be what you choose if you have server and client elements to your mod.
This is what you should use if you're a server only mod.
special case and should only be used if your mod has no server component.
more information. You can define any scheme you wish with this value.
on. Your mod should load (and maybe do nothing!) whereever it finds itself.

description = '''${mod_description}'''
[[dependencies."${mod_id}"]] #optional
modId = "forge" #mandatory
mandatory = true #mandatory
versionRange = "${forge_version_range}" #mandatory
ordering = "NONE"
side = "BOTH"# Here's another dependency
[[dependencies."${mod_id}"]]
modId = "minecraft"
mandatory = true
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"
